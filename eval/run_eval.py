"""QueryMind Evaluation Harness — Phase 4 stub.

Runs each NL question in eval_set.json through the pipeline, executes the
ground-truth SQL, compares result sets (not raw SQL text), and prints an
accuracy report broken down by category.

Usage (Phase 4):
    cd eval
    pip install -r requirements.txt
    python run_eval.py
"""

# Phase 4: implement evaluation harness.
